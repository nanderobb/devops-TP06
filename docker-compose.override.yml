version: "3.9"

services:
  backend:
    environment:
      APP_ENV: development
    # En dev, montar el código como volumen (hot reload)
    volumes:
      - ./backend/app.py:/app/app.py:ro
    command: ["python3", "-m", "flask", "run", "--host=0.0.0.0",
              "--port=5000", "--debug"]

 
